# HKJC Race Analysis — Python Dependencies
# Install: pip install -r requirements.txt

# Core data
pandas>=2.0
numpy>=1.24
scipy>=1.11             # SARR model uses scipy.stats.linregress
openpyxl>=3.1          # Excel read/write
matplotlib>=3.7        # Required by pandas Styler.background_gradient (Data Analysis heatmaps)
tabulate>=0.9          # Required by pandas DataFrame.to_markdown (factor_model_analysis regenerate)

# Network & scraping
requests>=2.31
beautifulsoup4>=4.12

# PDF generation
reportlab>=4.0

# Database (used by scrape_hkjc.py results scraper)
duckdb>=0.9

# Dashboard
streamlit>=1.35

# Modelling — gradient-boosted classifier (train_gbm.py)
lightgbm>=4.0
scikit-learn>=1.3       # GroupKFold for race-grouped CV

# Optional: headless browser fallback for JS-rendered pages
playwright>=1.40
